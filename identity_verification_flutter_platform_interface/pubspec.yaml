name: identity_verification_flutter_platform_interface
description: "A common platform interface for the identity_verification_flutter plugin."
version: 0.1.0
homepage: https://github.com/jeromexiong/identity_verification_flutter

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
